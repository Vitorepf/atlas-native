import SwiftUI
import WidgetKit
import ActivityKit
import AtlasCore

struct LockScreenView: View {
    let context: ActivityViewContext<AtlasTurnAttributes>

    var body: some View {
        lockScreenBody
    }
}
