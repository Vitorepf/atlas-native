import WidgetKit
import SwiftUI
import AtlasCore

// Lock accessory empty snapshot — peel de AtlasWidgetAccessories+LockLive.

extension LockAccessorySnapshotView {
    var lockAccessoryEmpty: some View {
        Text("abra o Atlas")
    }
}
