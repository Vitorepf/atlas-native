import SwiftUI
import AtlasCore

// Index rows block — peel de ArenaIndexSection+Content+Stack.

extension ArenaIndexSection {
    @ViewBuilder
    var indexContentRowsBlock: some View {
        engineRows
    }
}
