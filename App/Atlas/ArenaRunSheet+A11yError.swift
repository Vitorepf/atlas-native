import Foundation
import AtlasCore

// Error spoken — peel de ArenaRunSheet+A11yReceipt.

extension ArenaRunSheet {
    func spokenErrorLabel(_ message: String) -> String {
        "erro: \(message)"
    }
}
