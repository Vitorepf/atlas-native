import SwiftUI
import AtlasCore

// Botões individuais do menu options — peel de ComposerToolbar+OptionsButtons.
// Workspace → ComposerToolbar+OptionsWorkspace.swift
// Mode → ComposerToolbar+OptionsMode.swift
// Effort → ComposerToolbar+OptionsEffort.swift
