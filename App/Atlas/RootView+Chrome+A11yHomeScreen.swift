import SwiftUI

/// Home screen spoken — peel de RootView+Chrome+A11yHome.

extension RootView {
    func homeScreenSpokenLabel() -> String {
        "Atlas, início"
    }

    func homeScreenSpokenHint() -> String {
        "workspaces, conversas e Autônomos; Live Now aparece quando há sessão viva"
    }
}
