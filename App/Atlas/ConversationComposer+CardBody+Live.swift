import SwiftUI
import AtlasCore

// Live execution — peel de ConversationComposer+CardBody.

extension ConversationComposer {
    @ViewBuilder
    var composerCardBodyLive: some View {
        liveExecutionSection
    }
}
