import SwiftUI
import PhotosUI
import AtlasCore

// Ações do composer — peel de ConversationComposer (régua anti-inchaço).
// Dismiss → ConversationComposer+Actions+Dismiss.swift
// Send → ConversationComposer+Actions+Send.swift
