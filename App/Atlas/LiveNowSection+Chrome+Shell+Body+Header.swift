import SwiftUI
import AtlasCore

// Section header block — peel de LiveNowSection+Chrome+Shell+Body.
// Rows → LiveNowSection+Chrome+Shell+Body+Rows.swift

extension LiveNowSection {
    @ViewBuilder
    var liveNowSectionHeaderBlock: some View {
        header
    }
}
