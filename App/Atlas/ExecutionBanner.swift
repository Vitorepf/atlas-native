import Foundation

/// Spoken do banner de execução — peel de ConversationCockpit+ExecutionBanner (CICLO C).
/// Texto publicado pelo contrato; ícone decorativo.

enum ExecutionBannerA11y {
    static func spoken(text: String) -> String { text }
}
