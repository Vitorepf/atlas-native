import SwiftUI
import AtlasCore

// Section rows block — peel de LiveNowSection+Chrome+Shell+Body.

extension LiveNowSection {
    @ViewBuilder
    var liveNowSectionRowsBlock: some View {
        liveNowRows
    }
}
