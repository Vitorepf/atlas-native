import SwiftUI
import AtlasCore

// Index chart block — peel de ArenaIndexSection+Content+Stack.

extension ArenaIndexSection {
    @ViewBuilder
    var indexContentChartBlock: some View {
        indexContentChart
    }
}
