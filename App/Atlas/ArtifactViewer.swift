import SwiftUI
import UIKit
import AtlasCore

// Preview helpers do ArtifactSheet — fora do shell para a régua (~160).
// Preview → ArtifactViewer+Preview.swift
// Ficha → ArtifactFileFicha.swift
// Kind → ArtifactViewer+KindLabel.swift · Bytes → +ByteLabel.swift

enum ArtifactViewer {}
