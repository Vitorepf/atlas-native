import SwiftUI
import AtlasCore

// Chrome do grafo (status, worktrees, filtros, semana/recibo) — peel de
// AtlasCodeView+Graph. A lista de commits e a pílula ficam no Graph.
// Filters: AtlasCodeGraphChrome+Filters.swift · Status/week: +Status.swift
