# WAVE-024 compress

- `AtlasCodeRadarJudgment.sortedForJudgment` issues-first when scan hydrated
- Mute badge on repo rows (`mudo · não respondeu`)
- Pack top_attention subjects (slug + signal count)
- Folder rows receive isMuteFor from failedSlugs
