# WAVE-106 W3 — conversation-mid-thread-pack-live-hydration

A rank-1 max · mid-thread pack no longer hollow.

## What landed

- **`ConversationOccasionPack.PublishedSlice`** — presenceBubble · queued · agents.
- **`facts(..., published:)`** hydrates queue/plan/lanes/decision/can_do from model.
- **ConversationSurface** `rebindMidThreadTurnFacts` closes over model (Autônomos pattern).
- Session-only path keeps honest absence when unbound.
- CODEMAP mid-thread hydration organ.

DEVICE_PENDING. Gates green.
