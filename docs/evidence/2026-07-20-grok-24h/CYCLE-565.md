# CYCLE-565 — residual craft: gold-quiet disks + PressableScale family

## D1 Observe
Composer secondary chrome (attach/ellipsis) already soft surfaceHi disks; glass circle fallback bare; Arena selected tab pill elevation without gold rim; Autônomos map CTAs + Root CircleButton still `.plain`.

## D2 Leap
Gold-quiet residual craft on floating chrome family — no new product areas.

## D3 Cut
Zero Sources/** / models / package. Casca only.

## D4 Build
- ConversationView: attach + ellipsis `goldBorder.opacity(0.4)` rims on surfaceHi disks
- AtlasTheme: AtlasGlassCircle fallback gold-quiet stroke (match capsule)
- ArenaPremiumShell: selected tab pill gold rim + PressableScale + elevation opacity
- AutonomosView: primary map CTA + quiet CTA → PressableScale
- RootView: CircleButton → PressableScale

## D5 Prove
- `swift run AtlasCoreChecks` ✓
- `cd App && make build` ✓

## D6 Ledger
next_leap: residual craft (row PressableScale, micro elevation, glass consistency)
