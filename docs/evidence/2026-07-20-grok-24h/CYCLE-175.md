# CYCLE-175 — residual sheets/chrome isButton wave

## Change
- AtlasCloseToolbarButton shared close isButton (feeds every sheet).
- Nightly accept/dismiss/mute menu isButton.
- Arena stop confirm + spoken label; reason/steer submit isButton.
- Empty suggestions, review chip, scroll FAB isButton.
- Arena run toggles/submit, artifact rows isButton.
- Markdown copy, provenance ask/file, commit row isButton.

## Gates
checks+build · `3f40e519` · App/Atlas 214
