# CYCLE 257 — fuse vestment + delete identity a11y peel

## Edits
- `AutonomosHubVestment` → `AutonomosDestination.swift`; delete file
- Delete `ExecutionBanner.swift` (`spoken` was identity); cockpit uses text directly

## Gates
checks + make build
