# CYCLE 253 — surface Autônomos controlError

## Hipótese
Ciclo 252 escreve `controlError` no dry-run, mas a face v9 nunca renderizava — erro honesto invisível.

## Edit
- `AutonomosMapShell.catalogFace`: banner dismissable + a11y `autonomosControlError`
- soft haptic on dismiss; editorial animation; HIG ≥48

## Gates
checks + make build
